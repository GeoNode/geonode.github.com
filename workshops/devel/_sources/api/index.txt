.. _api:

GeoNode APIs
============

.. todo::

    From the notes::

        GeoNode's APIs
            OGS OWS (WMS, WFS, WCS, CSW, TMS, KML, GSR etc)
            GeoServer REST API
            GeoServer Import and Print API
            GeoNode Ad Hoc API
